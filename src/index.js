import React from 'react';
import ReactDOM from 'react-dom/client';
import GameBundle from './GameBundle/GameBundle';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GameBundle />
  </React.StrictMode>
);
